(() => { for (const i of ['xxl', 'xl', 'l', 'm', 'sm', 's']) { Font[i].family = 'Grandstander, cursive'; } })();

const Boot = Scene.create('Boot');
const Menu = Scene.create('Menu');
const Tuts = Scene.create('Tuts');
const Play = Scene.create('Play');